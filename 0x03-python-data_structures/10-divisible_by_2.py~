#!/usr/bin/python3
def divisible_by_2(my_list=[]):
    mlples = []
    for i in range(len(my_list)):
        if my_list[i] % 2 == 0:
             mlples.append(True)
        else:
             mlples.append(False)

    return ( mlples)
